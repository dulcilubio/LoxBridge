// Secrets.swift.template — copy this file to Secrets.swift and fill in real values.
// Secrets.swift is gitignored and should never be committed.

enum Secrets {
    static let liveloxClientId = "YOUR_LIVELOX_CLIENT_ID"
}
